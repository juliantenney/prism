# MathJax Test Fixture Plan

## Planned Fixture Types
- Inline math fixture (`\(...\)`)
- Block math fixture (`\[...\]`)
- Mixed markdown + math fixture
- Malformed delimiter fixture
- Export parity fixture (preview vs learner/facilitator export)
- Assessment math fixture (stem/options/explanations)

## Fixture Notes
- Include one long-page performance fixture.
- Include fallback readability fixture when typeset fails/unavailable.

## TODO
- [ ] Finalize fixture naming convention
- [ ] Map each fixture to phase checkpoints
