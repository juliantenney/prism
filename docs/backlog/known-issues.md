# Known Technical Debt

- duplicated logic in several UI/state pathways
- legacy code paths that remain for compatibility but reduce clarity

# Runtime Complexity

- state complexity accumulation across long-lived `app.js` surfaces
- mixed responsibilities between UI wiring and semantic workflow handling

# Domain-Pack Issues

- responsibility overlap across domain pack docs in some areas
- uneven granularity between principles, step patterns, and prompt rules

# Workflow Generation Weaknesses

- workflow-generation detail quality remains inconsistent
- workflow semantics are still evolving in generation pathways

# UX Friction

- inspectability gaps in some workflow/configuration flows
- occasional ambiguity in user-facing semantics for advanced options

