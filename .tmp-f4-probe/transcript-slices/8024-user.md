# line 8024 role=user

<timestamp>Wednesday, Sep 23, 2026, 1:03 PM (UTC+1)</timestamp>
<user_query>
S87 T-007 — Final Figure 4 visual-production diagnosis and bounded repair

Context

The post-refinement Bayes resource has now completed the frozen Expository QA workflow.

Results:

- Benchmark: 89/100
- Independent validator: Validated with revisions
- Validated final score: 90/100 — Professional / excellent
- No Critical or Major defects
- One confirmed Moderate production defect remains

The validator confirms that Figure 4, the base-rate comparison, visibly omits key numerical content:

- 1% prior → approximately 15.4% posterior
- 20% prior → approximately 81.8% posterior
- identical 90% likelihood/sensitivity and 5% false-positive rate across the comparison

The learner-facing prose, structured material and caption contain the correct numerical relationships, but the generated visual has blank posterior fields and substitutes qualitative lower/higher-prevalence labels for the intended numerical priors.

Important:

The previous Figure 3 connector defect has NOT recurred.

Do not assume the validator's suggested implementation ownership is correct.

This task is specifically to determine whether Figure 4 represents:

A. deterministic semantic information loss inside PRISM before image generation; or

B. failure of the image generator to realise an adequate explicit brief.

Do not begin by implementing a general visual-QA system.

--------------------------------------------------
1. TRACE THE ACTUAL FIGURE 4 PATH
--------------------------------------------------

Trace this specific Figure 4 from its authoritative semantic inputs through:

- XD material/representation commission;
- XM/structured material where relevant;
- DP visual planning;
- visual job;
- image-brief projection/compiler;
- final prompt/brief supplied for image generation;
- generated asset;
- learner-facing caption/description.

For each stage, report whether these exact quantities/relationships are present:

- prior/prevalence 1%;
- posterior approximately 15.4%;
- prior/prevalence 20%;
- posterior approximately 81.8%;
- sensitivity/likelihood 90%;
- false-positive rate 5%;
- explicit fact that the latter two remain constant while prevalence changes.

The key question is:

**Did PRISM explicitly require the generated image itself to display the four comparison values, or did that semantic precision disappear/weaken before generation?**

Do not infer from the caption or surrounding prose. Inspect the actual visual-generation brief.

--------------------------------------------------
2. CONDITIONAL ACTION
--------------------------------------------------

CASE A — deterministic PRISM information loss

If the exact comparison values/relationships are present upstream but absent or weakened in the image brief:

- identify the narrowest projection/contract seam responsible;
- repair that seam;
- preserve semantic values required for the explanatory purpose of the visual;
- make the fix generic to the semantic shape, not Bayes-specific;
- do not hard-code these particular numbers or terminology;
- add a regression test representing the actual failure shape.

The regression should prove that semantically required comparison values survive into the compiled image brief.

CASE B — adequate brief, stochastic image-generation failure

If the final image-generation brief already explicitly and unambiguously requires the image to show:

- 1% → ~15.4%;
- 20% → ~81.8%;
- with 90% and 5% held constant;

then DO NOT redesign PRISM merely because the generated image failed to obey that brief.

In that case:

- classify this as a stochastic visual-realisation failure;
- document it as such;
- do not add a new AI stage;
- do not add an image-inspection/vision QA subsystem;
- do not introduce retry machinery unless equivalent failures are already evidenced as recurrent;
- do not alter assembly or the structured-material renderer;
- do not change the Expository editorial prompts.

A manual/regenerated image may be used to establish that a subsequent generation can realise the existing brief, but this is not required to justify an architecture change.

CASE C — ambiguous/shared responsibility

If the brief technically contains the values but expresses them in a way that makes omission reasonably likely — for example buried JSON, weak optional wording, unclear must-show semantics, or loss of relationship between values — report that explicitly.

Make only the smallest generic brief-quality correction justified by the evidence.

--------------------------------------------------
3. PROTECT ACCEPTED S87 WORK
--------------------------------------------------

Do not reopen:

- EQ6 authorial-register work;
- supporting-material commissioning refinement;
- terminal closure;
- structured-material publication rendering;
- Figure 3 authorised-connector repair;
- typography;
- information architecture;
- accessibility-equivalence policy.

The QA validator now explicitly regards the remaining prose cadence as minor polish rather than implementation debt.

B and C from the previous refinement pass are accepted and frozen.

--------------------------------------------------
4. VALIDATION IF CODE CHANGES
--------------------------------------------------

If a deterministic fix is made:

1. add/run the focused regression for the actual Figure 4 failure shape;
2. run relevant image-brief/compiler tests;
3. run affected S87 tests;
4. confirm Interactive behaviour is either unchanged or, if shared code changes, that the shared change is independently correct;
5. report exact pass counts.

Do not run another Expository corpus.

Do not regenerate Bayes merely to chase a score.

A final full first-class gate can be run as part of S87 closure rather than repeatedly during diagnosis unless this repair changes sufficiently broad shared code to justify running it now.

--------------------------------------------------
5. DOCUMENTATION
--------------------------------------------------

Update T-007 with:

- Benchmark result: 89/100
- Independent validated result: 90/100 — Professional / excellent
- Figure 4 Moderate defect confirmed
- exact traced location at which its numerical semantics were preserved, weakened or lost;
- classification as deterministic PRISM defect, stochastic generation failure, or mixed/brief-quality issue;
- repair made, if any;
- tests, if any.

Also record:

- previous Figure 3 connector defect did not recur;
- EQ6 authorial-register refinement accepted;
- supporting-material explanatory-value refinement accepted;
- no further general editorial refinement is warranted by this validation.

Do not close Sprint 87 yet.

--------------------------------------------------
REPORT BACK
--------------------------------------------------

First report the trace in a compact table:

Stage | 1% | ~15.4% | 20% | ~81.8% | 90% | 5% | held-constant relationship | assessment

Then state:

1. classification: deterministic / stochastic / mixed;
2. exact root cause;
3. whether code should change;
4. proposed files/tests if code should change.

If deterministic or mixed, implement the smallest justified correction and report test results.

If stochastic with an already adequate brief, stop after documentation and report that no implementation change is justified.
</user_query>
