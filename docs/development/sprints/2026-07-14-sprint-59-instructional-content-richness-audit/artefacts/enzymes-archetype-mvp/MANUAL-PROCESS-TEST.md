# Manual process_walkthrough transfer test — handoff

**Purpose:** Evidence whether the archetype-contract pattern transfers from `mechanism_explanation` to `process_walkthrough`.  
**Status (2026-07-15):** Transfer test **PASS**. Final rule version **`20260715-4`**.

**Validated behaviour:** stage → reasoning performed → finding produced → finding transferred → conclusion.

**Opt-in token (required):** `S59_PROCESS_TEST`  
**Do not** enable `S59_MECHANISM_TEST` or leave `window.__PRISM_S59_MECHANISM_TEST = true` (both → fail closed / no test block).

---

## Result summary

| Gate | Status |
| ---- | ------ |
| DLA process emission / stamp | PASS |
| Contract persistence | PASS |
| Archetype routing selection | PASS |
| GAM Copy includes routing + process rule | PASS |
| Runtime cache-bust | PASS |
| Finding-transfer realisation (post-delivery) | PASS |

**Important history:** Earlier apparent process failures were often **invalid tests**. Outer GAM recognition used shaped context; inner routing used the raw step → `outer = true`, `inner = false`, routing skipped. The process rule never reached the clipboard. Fixed via `buildWorkflowStepRecognitionContext`. The rule itself was not the failure — delivery was.

---

## Manual gate — prove submitted Copy prompt has routing

Hard-reload:

```text
lib/ld-instructional-archetype.js?v=20260715-4
lib/workflow-step-recognition-context.js?v=20260715-s59-gam-ctx-1
app.js?v=20260715-s59-gam-ctx-1
```

In **Run mode**, select GAM → click **Copy**, then:

```js
const s = window.__PRISM_S59_FINAL_GAM_PROMPT;
const p = s?.prompt || "";
console.log({
  snapshotExists: Boolean(s),
  contains_archetype_routing: s?.contains_archetype_routing,
  contains_process_rule: s?.contains_process_rule,
  selected_instructional_archetype: s?.selected_instructional_archetype,
  hasProcessSelection: p.includes(
    "Selected rule ids for this request: process_walkthrough"
  ),
  hasContinuousWalkthrough: p.includes(
    "one continuous worked walkthrough"
  ),
  hasExplicitFinding: p.includes(
    "produce an explicit intermediate finding"
  ),
  hasFindingConsumption: p.includes(
    "Each later stage must consume information generated by earlier stages"
  ),
  hasAntiHowTo: p.includes(
    "do not restate stage labels as procedural Step/how-to instructions"
  ),
  prompt_length: s?.prompt_length
});
```

Gate:

```text
contains_archetype_routing === true
contains_process_rule === true
selected_instructional_archetype === "process_walkthrough"
SCRIPT_VERSION === "20260715-4"
```

Clipboard text MUST include:

```text
LD-INSTRUCTIONAL-ARCHETYPE-ROUTING
Selected rule ids for this request: process_walkthrough
one continuous worked walkthrough
produce an explicit intermediate finding
Each later stage must consume information generated by earlier stages
do not restate stage labels as procedural Step/how-to instructions
```

Do not paste into Copilot until this gate passes (re-runs / regression only).

---

## Manual gate — verify GAM rule BEFORE regenerating A4-M1

```js
console.log(window.PRISM_LD_INSTRUCTIONAL_ARCHETYPE.SCRIPT_VERSION);
// → must be "20260715-4"

window.__PRISM_TEST_API.captureS59GamArchetypeRuntimeSnapshot();
console.log(window.__PRISM_S59_GAM_ARCHETYPE_RUNTIME);
```

Gate pass:

```json
{
  "loaded_archetype_script_version": "20260715-4",
  "selected_instructional_archetype": "process_walkthrough",
  "material_id": "A4-M1",
  "revised_rule_present": true,
  "old_rule_present": false
}
```

`selected_rule_text` MUST start with `Realise the process_goal as one continuous worked walkthrough`.

MUST NOT: `Walk through the supplied stages in order`

---

## Manual gate — verify activation BEFORE running DLA

In the browser console:

```js
window.__PRISM_S59_MECHANISM_TEST = false;
window.__PRISM_S59_PROCESS_TEST = false;

console.log({
  archetype: window.PRISM_LD_INSTRUCTIONAL_ARCHETYPE &&
    window.PRISM_LD_INSTRUCTIONAL_ARCHETYPE.SCRIPT_VERSION,
  hasProcessApi: !!(window.__PRISM_TEST_API &&
    window.__PRISM_TEST_API.resolveS59DlaTestActivation),
  appScript: [...document.scripts].map(s => s.src).filter(s =>
    /app\.js|ld-instructional-archetype|workflow-step-recognition/.test(s)
  )
});

window.__PRISM_TEST_API.resolveS59DlaTestActivation(
  { goal: document.querySelector('#workflowGoal')?.value || '' }
);
console.log(window.__PRISM_S59_DLA_TEST_ACTIVATION);
```

Gate pass criteria:

```json
{
  "workflow_goal": "... must include S59_PROCESS_TEST ...",
  "mechanism_requested": false,
  "process_requested": true,
  "selected_dla_test": "process",
  "loaded_archetype_script_version": "20260715-4"
}
```

---

## Designated material

| Field | Value |
| ----- | ----- |
| `activity_id` | `A4` |
| `material_id` | `A4-M1` |
| `material_type` | `worked_example` |
| `instructional_archetype` | `process_walkthrough` |

**Why process (not mechanism):** Ordered investigation-interpretation (identify → inspect → connect → conclude), not a causal start→outcome chain.

**Plan:**

```json
{
  "instructional_archetype": "process_walkthrough",
  "archetype_plan": {
    "process_goal": "interpret an enzyme reaction-rate investigation",
    "stages": [
      "identify the manipulated condition and measured outcome",
      "inspect the pattern across observations",
      "connect the pattern to enzyme behaviour",
      "form a bounded conclusion"
    ]
  }
}
```

---

## How to trigger (regression re-run)

1. Open the existing enzymes (or equivalent) workflow — do not create a new workflow.
2. Put in workflow **goal** (exactly one token):

```text
Enzymes — S59_PROCESS_TEST
```

3. Clear `window.__PRISM_S59_MECHANISM_TEST`. Prefer goal token over window flags.
4. Pass the manual gates above.
5. Open **Design Learning Activities** / GAM as needed.

---

## Exact DLA prompt markers (process run)

**MUST contain:**

```text
LD-INSTRUCTIONAL-ARCHETYPE-DLA-PROCESS-TEST
S59_PROCESS_TEST is active
```

**MUST NOT contain:**

```text
LD-INSTRUCTIONAL-ARCHETYPE-DLA-MECHANISM-TEST
S59_MECHANISM_TEST is active
```

---

## Blockers (regression)

| Symptom | Likely cause |
| ------- | ------------ |
| Mechanism markers still appear | Goal/notes still contain `S59_MECHANISM_TEST`, or stale `window.__PRISM_S59_MECHANISM_TEST === true` |
| No Sprint 59 block | Neither token, or both tokens → conflict fail-closed |
| Snapshot missing / no routing in Copy | Stale `app.js` — hard-reload for `?v=20260715-s59-gam-ctx-1` |
| `SCRIPT_VERSION` ≠ `20260715-4` | Stale archetype lib — hard-reload for `?v=20260715-4` |
| Old process rule still in GAM prompt | Saved local override with baked-in stale routing block — rebuild GAM prompt after reload |
